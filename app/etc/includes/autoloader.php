<?php

// spl_autoload_register(function ($class) {
//     $class = ucwords($class, '_'); // Fix for classes with incorrect capitalization
//     if ($file = stream_resolve_include_path(strtr($class, '_', DS) . '.php')) {
//         return include $file;
//     }
// });
