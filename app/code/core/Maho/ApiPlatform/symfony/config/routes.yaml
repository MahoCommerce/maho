api_platform:
    resource: .
    type: api_platform
    prefix: /api

# Admin GraphQL controller (registered via attribute routing)
admin_graphql:
    resource: ../src/Controller/
    type: attribute
