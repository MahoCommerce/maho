<?php
/**
 * Maho
 *
 * @package     default_default
 * @copyright   Copyright (c) 2006-2020 Magento, Inc. (https://magento.com)
 * @copyright   Copyright (c) 2021-2024 The OpenMage Contributors (https://openmage.org)
 * @copyright   Copyright (c) 2024-2026 Maho (https://mahocommerce.com)
 * @license     https://opensource.org/licenses/afl-3.0.php  Academic Free License (AFL 3.0)
 */

/** @var Mage_Adminhtml_Block_Dashboard $this */
?>

<?php if (is_array($this->getChild('diagrams')->getTabsIds())) : ?>
<script type="text/javascript">
//<![CDATA[
function changeDiagramsPeriod(periodObj) {
    periodParam = periodObj.value ? 'period/' + periodObj.value + '/' : '';
<?php foreach ($this->getChild('diagrams')->getTabsIds() as $tabId): ?>
    ajaxBlockParam = 'block/tab_<?= $tabId ?>/';
    ajaxBlockUrl = '<?= $this->getUrl('*/*/ajaxBlock', ['_current' => true, 'block' => '', 'period' => '']) ?>' + ajaxBlockParam + periodParam;
    const formData_<?= $tabId ?> = new FormData();
    formData_<?= $tabId ?>.append('isAjax', 'true');
    formData_<?= $tabId ?>.append('form_key', FORM_KEY);

    mahoFetch(ajaxBlockUrl, {
        method: 'POST',
        body: formData_<?= $tabId ?>
    })
    .then(response => {
        tabContentElementId = '<?= $this->getChild('diagrams')->getId() ?>_<?= $tabId ?>_content';
        try {
            let responseData = response;
            if (typeof response === 'string') {
                try {
                    responseData = JSON.parse(response);
                    if (responseData.error) {
                        alert(responseData.message);
                    }
                    if(responseData.ajaxExpired && responseData.ajaxRedirect) {
                        setLocation(responseData.ajaxRedirect);
                    }
                } catch (e) {
                    document.getElementById(tabContentElementId).innerHTML = response;
                }
            } else {
                document.getElementById(tabContentElementId).innerHTML = response;
            }
        }
        catch (e) {
            document.getElementById(tabContentElementId).innerHTML = response;
        }
    })
    .catch(error => {
        console.error('Ajax block error:', error);
    });
<?php endforeach ?>
    ajaxBlockUrl = '<?= $this->getUrl('*/*/ajaxBlock', ['_current' => true, 'block' => 'totals', 'period' => '']) ?>' + periodParam;
    const formData2 = new FormData();
    formData2.append('isAjax', 'true');
    formData2.append('form_key', FORM_KEY);

    mahoFetch(ajaxBlockUrl, {
        method: 'POST',
        body: formData2
    })
    .then(response => {
        tabContentElementId = 'dashboard_diagram_totals';
        try {
            let responseData = response;
            if (typeof response === 'string') {
                try {
                    responseData = JSON.parse(response);
                    if (responseData.error) {
                        alert(responseData.message);
                    }
                    if(responseData.ajaxExpired && responseData.ajaxRedirect) {
                        setLocation(responseData.ajaxRedirect);
                    }
                } catch (e) {
                    document.getElementById(tabContentElementId).outerHTML = response;
                }
            } else {
                document.getElementById(tabContentElementId).outerHTML = response;
            }
        }
        catch (e) {
            document.getElementById(tabContentElementId).outerHTML = response;
        }
    })
    .catch(error => {
        console.error('Ajax block totals error:', error);
    });
}

function toggleCal(id) {
    const calDiv = document.getElementById('dashboard_'+id+'_cal_div');
    const rangeDiv = document.getElementById('dashboard_'+id+'_range_div');
    calDiv.style.display = calDiv.style.display === 'none' ? 'block' : 'none';
    rangeDiv.style.display = rangeDiv.style.display === 'none' ? 'block' : 'none';
}
//]]>
</script>
<?php endif ?>
<div class="content-header">
    <h3 class="head-dashboard"><?= $this->__('Dashboard') ?></h3>
</div>
<div class="dashboard-container">
    <?= $this->getChildHtml('store_switcher') ?>
    <div class="dashboard-grid">
        <div class="dashboard-col-left">
            <?php if ($this->getChild('visitorStats')): ?>
            <div class="entry-edit">
                <div class="entry-edit-head"><h4><?= $this->__('Visitors') ?></h4></div>
                <fieldset class="np"><?= $this->getChildHtml('visitorStats') ?></fieldset>
            </div>
            <?php endif ?>
            <?php if ($this->getChild('sessionMetrics')): ?>
            <div class="entry-edit">
                <div class="entry-edit-head"><h4><?= $this->__('Session Metrics') ?></h4></div>
                <fieldset class="np"><?= $this->getChildHtml('sessionMetrics') ?></fieldset>
            </div>
            <?php endif ?>
            <?= $this->getChildHtml('sales') ?>
            <div class="entry-edit">
                <div class="entry-edit-head"><h4><?= $this->__('Last 5 Orders') ?></h4></div>
                <fieldset class="np"><?= $this->getChildHtml('lastOrders') ?></fieldset>
            </div>
            <div class="entry-edit">
                <div class="entry-edit-head"><h4><?= $this->__('Last 5 Search Terms') ?></h4></div>
                <fieldset class="np"><?= $this->getChildHtml('lastSearches') ?></fieldset>
            </div>
            <div class="entry-edit">
                <div class="entry-edit-head"><h4><?= $this->__('Top 5 Search Terms') ?></h4></div>
                <fieldset class="np"><?= $this->getChildHtml('topSearches') ?></fieldset>
            </div>
        </div>
        <div class="dashboard-col-right">
            <div class="entry-edit dashboard-diagrams-box">
                <?= $this->getChildHtml('diagrams') ?>
                <?php if (is_array($this->getChild('diagrams')->getTabsIds())) : ?>
                    <div id="diagram_tab_content"></div>
                <?php endif ?>
                <div class="dashboard-totals-wrapper">
                    <?= $this->getChildHtml('totals') ?>
                </div>
            </div>
            <div class="entry-edit dashboard-grids-box">
                <?= $this->getChildHtml('grids') ?>
                <div id="grid_tab_content"></div>
            </div>
        </div>
    </div>
</div>
