<?php
/**
 * SPDX-FileCopyrightText: 2024-2026 Maho <https://mahocommerce.com>
 * SPDX-FileCopyrightText: 2021-2022 The OpenMage Contributors <https://openmage.org>
 * SPDX-FileCopyrightText: 2006-2020 Magento, Inc. <https://magento.com>
 * SPDX-License-Identifier: AFL-3.0
 * @package default_default
 */
?>
<p class="switcher"><label for="store_switcher"><?= $this->__('View Statistics For:') ?></label>
<select name="store_switcher" id="store_switcher" class="left-col-block" onchange="return switchStore(this);">
    <option value=""><?= $this->__('All Websites') ?></option>
    <?php foreach ($this->getWebsiteCollection() as $_website): ?>
        <?php $showWebsite=false; ?>
        <?php foreach ($this->getGroupCollection($_website) as $_group): ?>
            <?php $showGroup=false; ?>
            <?php foreach ($this->getStoreCollection($_group) as $_store): ?>
                <?php if ($showWebsite == false): ?>
                    <?php $showWebsite = true; ?>
                    <option website="true" value="<?= $_website->getId() ?>"<?php if($this->getRequest()->getParam('website') == $_website->getId()): ?> selected="selected"<?php endif ?>><?= $this->escapeHtml($_website->getName()) ?></option>
                <?php endif ?>
                <?php if ($showGroup == false): ?>
                    <?php $showGroup = true; ?>
                    <!--optgroup label="&nbsp;&nbsp;&nbsp;<?= $this->escapeHtml($_group->getName()) ?>"-->
                    <option group="true" value="<?= $_group->getId() ?>"<?php if($this->getRequest()->getParam('group') == $_group->getId()): ?> selected="selected"<?php endif ?>>&nbsp;&nbsp;&nbsp;<?= $this->escapeHtml($_group->getName()) ?></option>
                <?php endif ?>
                <option value="<?= $_store->getId() ?>"<?php if($this->getStoreId() == $_store->getId()): ?> selected="selected"<?php endif ?>>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<?= $this->escapeHtml($_store->getName()) ?></option>
            <?php endforeach ?>
            <?php if ($showGroup): ?>
                <!--</optgroup>-->
            <?php endif ?>
        <?php endforeach ?>
    <?php endforeach ?>
</select>
</p>
<script>
//<![CDATA[
    function switchStore(obj){
        const option = obj.options[obj.selectedIndex];
        const selectionType = option.getAttribute('website') == 'true' ? 'website'
            : (option.getAttribute('group') == 'true' ? 'group' : 'store');

        // The scope params are read in store/website/group order, so the unselected ones
        // must be dropped rather than left over from the current URL.
        const params = { website: null, group: null, store: null };
        if (obj.value) {
            params[selectionType] = obj.value;
        }
        if (document.getElementById('diagram_tab_orders_content').style.display != 'none') {
            var select = document.getElementById('order_orders_period');
        } else if (document.getElementById('diagram_tab_amounts_content').style.display != 'none') {
            var select = document.getElementById('order_amounts_period');
        }
        params.period = select.value || null;

        setLocation(setRouteParams('<?= $this->getSwitchUrl() ?>', {
            ...params,
            ...parseRouteParams(obj.switchParams),
        }));
    }
//]]>
</script>
