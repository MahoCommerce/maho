# PayPal sandbox credentials for the browser/integration test suites.
# Copy this file to .env.testing and fill in your sandbox app credentials.
# .env.testing is gitignored; CI provides these as secrets instead.
PAYPAL_SANDBOX_CLIENT_ID=
PAYPAL_SANDBOX_CLIENT_SECRET=
